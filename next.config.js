/** @type {import('next').NextConfig} */
const nextConfig = {
  // Removed deprecated appDir configuration
  // Next.js 14 uses app directory by default when present
  // Force fresh Vercel deployment
}

module.exports = nextConfig
